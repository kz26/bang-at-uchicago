// Generated by CoffeeScript 1.6.2
app.directive('validatePassword', function() {
  return {
    require: 'ngModel',
    link: function(scope, elem, attrs, ctrl) {
      scope.$watch(function(s) {
        return s.$eval(attrs.validateWith);
      }, function() {
        if (attrs.validateWith === ctrl.$viewValue) {
          return ctrl.$setValidity('validatePassword', true);
        } else {
          return ctrl.$setValidity('validatePassword', false);
        }
      });
      return ctrl.$parsers.unshift(function(viewValue) {
        var pw1, pw2;

        pw1 = attrs.validateWith;
        pw2 = viewValue;
        if (pw1 === pw2) {
          ctrl.$setValidity('validatePassword', true);
          return viewValue;
        } else {
          ctrl.$setValidity('validatePassword', false);
          return void 0;
        }
      });
    }
  };
});

app.directive('tooltip', function($parse) {
  return {
    link: function(scope, elem, attrs) {
      if ('caption' in attrs) {
        attrs.$set('title', $parse(attrs.caption)(scope));
      }
      return elem.tooltip();
    }
  };
});

app.directive('bool', function() {
  return function(scope, elem, attr) {
    return attr.$set('value', attr.bool === "true");
  };
});
